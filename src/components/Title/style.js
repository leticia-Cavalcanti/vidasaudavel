import { StyleSheet } from "react-native";

//constante de estilo e alinhamento dos textos
const styles = StyleSheet.create({
    boxTitle:{
        alignItems: "center",
        justifyContent: "center",
        padding: 10,
    },
    textTitle:{
        color: "#FF0043",
        fontSize: 24,
        fontWeight: "bold",
    }
});

export default styles

